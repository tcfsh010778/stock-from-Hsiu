name: Publish Human Review Home

on:
  workflow_dispatch:
  schedule:
    - cron: '0 11,13 * * 1-5'
  push:
    branches: [main]
    paths:
      - 'build_review_data.py'
      - 'review_queue.py'
      - 'review_home.py'
      - 'mda_weekly_pipeline.py'
      - 'mda_checklist_page.py'
      - 'sfz_technical_pipeline.py'
      - 'stock_v2_public/analysis/mda_checklist.py'
      - 'stock_v2_public/analysis/sfz_universe.py'
      - 'stock_v2_public/analysis/sfz_review.py'
      - '.github/workflows/review_home_publish.yml'

permissions:
  contents: write
  pages: write

concurrency:
  group: daily-stock-site-update
  cancel-in-progress: false

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python -m pip install -r requirements-v2.lock
      - name: Sync latest generated data
        run: git pull --ff-only origin main
      - name: Build independent observations and compare durable state
        run: |
          PRICE_DATE=$(python tools/resolve_verified_session.py)
          python build_review_data.py --as-of "$PRICE_DATE"
      - name: Build only review homepage and supporting pages
        run: python generate_site.py --review-only
      - name: Verify source gates and publication semantics
        run: python tools/verify_review_home.py
      - name: Commit scoped public results
        id: publish
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add data/review_queue.json data/sfz_technical_candidates.json data/mda_checklist_candidates.json docs/data/review_queue.json docs/data/sfz_technical_candidates.json docs/data/mda_checklist_candidates.json docs/data/mda_weekly_top50.json docs/index.html docs/review-pool.html docs/review-rules.html docs/mda-checklist.html docs/css/review-home.css docs/js/review-home.js
          if git diff --cached --quiet; then
            echo 'changed=false' >> "$GITHUB_OUTPUT"
            exit 0
          fi
          git commit -m 'Update independent routes and human review home'
          git pull --rebase origin main
          git push
          echo 'changed=true' >> "$GITHUB_OUTPUT"
      - name: Request Pages build for generated commit
        if: steps.publish.outputs.changed == 'true'
        env:
          GH_TOKEN: ${{ github.token }}
        run: python tools/publish_pages_verified.py --repo "${{ github.repository }}" --docs-dir docs
